export const environment = {
  production: false,
  apiBaseUrl: '/api/v1',
  // ⚠️ Esta key corresponde al kiosco inicial insertado por seed.ts en desarrollo.
  // En producción, usa un archivo local que contenga la key real.
  kioskApiKey: 'KIOSK_DEMO_KEY_123',
};
